interface:
  display_name: "Dispatch Cutover"
  short_description: "Convert wrapper walls to dispatch paths."
  default_prompt: "Use $renderer-dispatch-cutover to replace extracted wrapper walls with grouped dispatch object calls. Keep all edits within dictionary-desktop project scope and local workflow files only. Keep all edits within aio project scope and local workflow files only."
policy:
  allow_implicit_invocation: true
