interface:
  display_name: "Dispatch Cutover"
  short_description: "Convert wrapper walls to dispatch paths."
  default_prompt: "Use $renderer-dispatch-cutover for this task. Stay in aio project scope only."
policy:
  allow_implicit_invocation: true
