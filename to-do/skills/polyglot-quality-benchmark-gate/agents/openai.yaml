interface:
  display_name: "Polyglot Quality Benchmark Gate"
  short_description: "Run strict tests, security checks, and SxS benchmarks."
  default_prompt: "Use $polyglot-quality-benchmark-gate to run strict correctness/security validation and side-by-side runtime/size benchmark comparisons. Keep all edits within dictionary-desktop project scope and local workflow files only. Keep all edits within aio project scope and local workflow files only."
policy:
  allow_implicit_invocation: true
