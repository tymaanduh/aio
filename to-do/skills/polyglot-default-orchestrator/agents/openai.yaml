interface:
  display_name: "Polyglot Default Orchestrator"
  short_description: "Run default blueprint-to-benchmark polyglot workflow."
  default_prompt: "Use $polyglot-default-orchestrator to run the default director flow: plan, enforce JSON instruction catalogs, translate, optimize, and benchmark/security-gate all candidate languages. Keep all edits within dictionary-desktop project scope and local workflow files only. Keep all edits within aio project scope and local workflow files only."
policy:
  allow_implicit_invocation: true
