interface:
  display_name: "Strict Cleanup"
  short_description: "Enforce strict pattern and registry format."
  default_prompt: "Use $strict-format-cleanup to enforce PATTERN_ extraction, registry naming, and dispatch-first dedupe. Keep all edits within dictionary-desktop project scope and local workflow files only. Keep all edits within aio project scope and local workflow files only."
policy:
  allow_implicit_invocation: true
