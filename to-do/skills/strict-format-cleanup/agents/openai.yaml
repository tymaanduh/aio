interface:
  display_name: Strict Cleanup
  short_description: Enforce strict pattern and registry format.
  default_prompt: Use $strict-format-cleanup for this task. Stay in aio project scope only.
policy:
  allow_implicit_invocation: true
