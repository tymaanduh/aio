interface:
  display_name: Linked Naming Governor
  short_description: Enforce semantic linked naming across scopes.
  default_prompt: Use $linked-naming-governor for this task. Stay in aio project scope only.
policy:
  allow_implicit_invocation: true
