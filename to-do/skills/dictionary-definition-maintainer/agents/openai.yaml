interface:
  display_name: "Dictionary Definition Maintainer"
  short_description: "Normalize and expand word definitions."
  default_prompt: "Use $dictionary-definition-maintainer to create/update dictionary entries with deterministic word and definition quality. Keep all edits within dictionary-desktop project scope and local workflow files only. Keep all edits within aio project scope and local workflow files only."
policy:
  allow_implicit_invocation: true
