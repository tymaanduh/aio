interface:
  display_name: "Renderer Modularizer"
  short_description: "Extract renderer domains into modules."
  default_prompt: "Use $renderer-wrapper-modularizer for this task. Stay in aio project scope only."
policy:
  allow_implicit_invocation: true
