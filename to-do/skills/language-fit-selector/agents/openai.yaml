interface:
  display_name: "Language Fit Selector"
  short_description: "Rank languages by runtime, size, and platform fit."
  default_prompt: "Use $language-fit-selector to detect available toolchains, score language options, and select a primary plus benchmark language set. Stay in aio project scope only."
policy:
  allow_implicit_invocation: true
