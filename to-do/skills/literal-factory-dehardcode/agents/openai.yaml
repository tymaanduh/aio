interface:
  display_name: "Literal Factory"
  short_description: "Replace hardcoded labels with factories."
  default_prompt: "Use $literal-factory-dehardcode to convert repeated hardcoded text into PATTERN maps and factory helpers. Keep all edits within dictionary-desktop project scope and local workflow files only. Keep all edits within aio project scope and local workflow files only."
policy:
  allow_implicit_invocation: true
