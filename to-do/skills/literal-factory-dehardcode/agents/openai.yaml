interface:
  display_name: Literal Factory
  short_description: Replace hardcoded labels with factories.
  default_prompt: Use $literal-factory-dehardcode for this task. Stay in aio project scope only.
policy:
  allow_implicit_invocation: true
