interface:
  display_name: "Word Machine Descriptor Compiler"
  short_description: "Map every token to pseudo-code machine descriptors."
  default_prompt: "Use $word-machine-descriptor-compiler to convert every extracted word token into deterministic opcode and pseudo-instruction descriptors. Keep all edits within dictionary-desktop project scope and local workflow files only. Keep all edits within aio project scope and local workflow files only."
policy:
  allow_implicit_invocation: true
