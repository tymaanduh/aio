interface:
  display_name: "Refactor Gate"
  short_description: "Run blocking checks for refactor waves."
  default_prompt: "Use $refactor-blocking-gate to run shape, alignment, lint, and test checks before completing refactors. Keep all edits within dictionary-desktop project scope and local workflow files only. Keep all edits within aio project scope and local workflow files only."
policy:
  allow_implicit_invocation: true
