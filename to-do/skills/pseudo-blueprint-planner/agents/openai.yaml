interface:
  display_name: "Pseudo Blueprint Planner"
  short_description: "Generate complete English and pseudocode app plans."
  default_prompt: "Use $pseudo-blueprint-planner to produce a complete English blueprint, JSON data catalog schemas, and module-by-module pseudocode before writing language-specific code."

policy:
  allow_implicit_invocation: true
