interface:
  display_name: "Alias Sync"
  short_description: "Keep alias index and docs aligned."
  default_prompt: "Use $dictionary-alias-sync to update alias runtime and documentation entries in the same pass. Stay in aio project scope only."
policy:
  allow_implicit_invocation: true
