interface:
  display_name: "Alias Sync"
  short_description: "Keep alias index and docs aligned."
  default_prompt: "Use $dictionary-alias-sync to update alias runtime and documentation entries in the same pass. Keep all edits within dictionary-desktop project scope and local workflow files only. Keep all edits within aio project scope and local workflow files only."
policy:
  allow_implicit_invocation: true
